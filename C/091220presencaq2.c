#include <stdio.h>
#include <stdlib.h>
int main(){
  int x, y, c;
  printf("Digite o tamanho do triângulo: ");
  scanf("%d", &x);
  for (x = 1; x <= c; x++)
      for (y = 1; y <= x; y = y + 1)
       printf("%d", x);
          printf(" \n");
  
}